// it used for private the method and variable
// behind the seen
//console()
(
  function(exports,require, module,__filename,__dirname)
  {
    // every thing are written inside this by the help of node but cann't see, like
    const name ="manoj kumar mandal";
    console.log(__dirname);
    console.log(__filename);
    console.log(name);
    module.exports={mkks}
  }
)